# Optional local assistant bridge settings.
# The bridge runs in mock mode when HF_TOKEN is empty.
HF_TOKEN=
HF_MODEL_NAME=google/gemma-2-27b-it
BRIDGE_HOST=127.0.0.1
BRIDGE_PORT=8001
BRIDGE_CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
